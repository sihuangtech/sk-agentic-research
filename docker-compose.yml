services:
  app:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./data/workspace:/app/data/workspace
      - ./config.yaml:/app/config.yaml
    env_file:
      - .env
    restart: unless-stopped
